/**
 * `$lib/theory` — the only module that imports `tonal` (ADR 0001 §4).
 * Everything else imports from here.
 */

export * from './types';
export * from './notes';
export * from './chords';
export * from './intervals';
export * from './voicings';
